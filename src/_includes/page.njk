---
layout: base.njk
---
<article class="page">
  <header class="page-header">
    <h1>{{ title }}</h1>
    {% if lede %}
    <p class="lede">{{ lede }}</p>
    {% endif %}
  </header>
  <div class="prose">
    {{ content | safe }}
  </div>
</article>
